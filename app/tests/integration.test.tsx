// import { render, fireEvent, screen } from "@testing-library/react";
// import "@testing-library/jest-dom";
// import Home from "../page";

// test("renders all lottery types on the home page", async () => {
//   render(<Home />);

//   // Use screen queries to check if all types are rendered
//   expect(screen.getByText("COSMIC")).toBeInTheDocument();
//   expect(screen.getByText("CLASSIC")).toBeInTheDocument();
//   expect(screen.getByText("ATOMIC")).toBeInTheDocument();
// });
